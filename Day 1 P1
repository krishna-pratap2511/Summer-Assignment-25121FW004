#include<stdio.h>
int main()
{
    int i, n,sum=0;
    printf("Enter the number till which you want the sum");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("The Sum upto %d is:%d",n,sum);
    return 0;

}

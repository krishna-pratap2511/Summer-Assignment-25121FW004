#include<stdio.h>
int main()
{
    int rev=0,n,r,num;
    printf("Enter any Number");
    scanf("%d",&n);
    num=n;
    while(n>0)
    {
        r=n%10;
        rev=rev*10+r;
        n=n/10;
    }
    printf("Reverse of %d is %d", num, rev);
    return 0;
}
